kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  labels:
      app: powerapp-db-volume
  name: powerapp-db-volume
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 6Gi
